<VirtualHost *:80>
#  Laravelプロジェクトの場合はpublicフォルダをドキュメントルートに設定する
    DocumentRoot /var/www/html/dockerpractice/public
    ServerName localhost
    <Directory "/var/www/html/dockerpractice">
        Allowoverride All
        Require all granted
    </Directory>
</VirtualHost>
